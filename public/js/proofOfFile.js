
$('#chairmanship_university').keyup(function() {

    // If value is not empty
    if ($(this).val().length == 0) {
      // Hide the element
      $('.chairmanship_university').hide();

    } else {
      // Otherwise show it
      $('.chairmanship_university').show();
    }
  }).keyup();

  $('#chairmanship_college').keyup(function() {

    // If value is not empty
    if ($(this).val().length == 0) {
      // Hide the element
      $('.chairmanship_college').hide();

    } else {
      // Otherwise show it
      $('.chairmanship_college').show();
    }
  }).keyup();


  $('#membership_university').keyup(function() {

    // If value is not empty
    if ($(this).val().length == 0) {
      // Hide the element
      $('.membership_university').hide();

    } else {
      // Otherwise show it
      $('.membership_university').show();
    }
  }).keyup();

  $('#membership_college').keyup(function() {

    // If value is not empty
    if ($(this).val().length == 0) {
      // Hide the element
      $('.membership_college').hide();

    } else {
      // Otherwise show it
      $('.membership_college').show();
    }
  }).keyup();

  $('#advisorship').keyup(function() {

    // If value is not empty
    if ($(this).val().length == 0) {
      // Hide the element
      $('.advisorship').hide();

    } else {
      // Otherwise show it
      $('.advisorship').show();
    }
  }).keyup();

  $('#oic').keyup(function() {

    // If value is not empty
    if ($(this).val().length == 0) {
      // Hide the element
      $('.oic').hide();

    } else {
      // Otherwise show it
      $('.oic').show();
    }
  }).keyup();

  $('#judge').keyup(function() {

    // If value is not empty
    if ($(this).val().length == 0) {
      // Hide the element
      $('.judge').hide();

    } else {
      // Otherwise show it
      $('.judge').show();
    }
  }).keyup();

  $('#resource').keyup(function() {

    // If value is not empty
    if ($(this).val().length == 0) {
      // Hide the element
      $('.resource').hide();

    } else {
      // Otherwise show it
      $('.resource').show();
    }
  }).keyup();

  $('#chairmanship_membership').keyup(function() {

    // If value is not empty
    if ($(this).val().length == 0) {
      // Hide the element
      $('.chairmanship_membership').hide();

    } else {
      // Otherwise show it
      $('.chairmanship_membership').show();
    }
  }).keyup();

  $('#facilication_on_going').keyup(function() {

    // If value is not empty
    if ($(this).val().length == 0) {
      // Hide the element
      $('.facilication_on_going').hide();

    } else {
      // Otherwise show it
      $('.facilication_on_going').show();
    }
  }).keyup();

  $('#facilication_regional').keyup(function() {

    // If value is not empty
    if ($(this).val().length == 0) {
      // Hide the element
      $('.facilication_regional').hide();

    } else {
      // Otherwise show it
      $('.facilication_regional').show();
    }
  }).keyup();

  $('#facilication_national').keyup(function() {

    // If value is not empty
    if ($(this).val().length == 0) {
      // Hide the element
      $('.facilication_national').hide();

    } else {
      // Otherwise show it
      $('.facilication_national').show();
    }
  }).keyup();

  $('#facilication_international').keyup(function() {

    // If value is not empty
    if ($(this).val().length == 0) {
      // Hide the element
      $('.facilication_international').hide();

    } else {
      // Otherwise show it
      $('.facilication_international').show();
    }
  }).keyup();

  $('#training_director_local').keyup(function() {

    // If value is not empty
    if ($(this).val().length == 0) {
      // Hide the element
      $('.training_director_locals').hide();

    } else {
      // Otherwise show it
      $('.training_director_locals').show();
    }
  }).keyup();

  $('#training_director_international').keyup(function() {

    // If value is not empty
    if ($(this).val().length == 0) {
      // Hide the element
      $('.training_director_internationals').hide();

    } else {
      // Otherwise show it
      $('.training_director_internationals').show();
    }
  }).keyup();


  $('#resource_speaker_local').keyup(function() {

    // If value is not empty
    if ($(this).val().length == 0) {
      // Hide the element
      $('.resource_speaker_locals').hide();

    } else {
      // Otherwise show it
      $('.resource_speaker_locals').show();
    }
  }).keyup();

  $('#resource_speaker_international').keyup(function() {

    // If value is not empty
    if ($(this).val().length == 0) {
      // Hide the element
      $('.resource_speaker_internationals').hide();

    } else {
      // Otherwise show it
      $('.resource_speaker_internationals').show();
    }
  }).keyup();


  $('#facilitator_moderator_local').keyup(function() {

    // If value is not empty
    if ($(this).val().length == 0) {
      // Hide the element
      $('.facilitator_moderator_locals').hide();

    } else {
      // Otherwise show it
      $('.facilitator_moderator_locals').show();
    }
  }).keyup();

  $('#facilitator_moderator_international').keyup(function() {

    // If value is not empty
    if ($(this).val().length == 0) {
      // Hide the element
      $('.facilitator_moderator_internationals').hide();

    } else {
      // Otherwise show it
      $('.facilitator_moderator_internationals').show();
    }
  }).keyup();


  $('#reactor_panel_member_international').keyup(function() {

    // If value is not empty
    if ($(this).val().length == 0) {
      // Hide the element
      $('.reactor_panel_member_internationals').hide();

    } else {
      // Otherwise show it
      $('.reactor_panel_member_internationals').show();
    }
  }).keyup();

  $('#reactor_panel_member_local').keyup(function() {

    // If value is not empty
    if ($(this).val().length == 0) {
      // Hide the element
      $('.reactor_panel_member_locals').hide();

    } else {
      // Otherwise show it
      $('.reactor_panel_member_locals').show();
    }
  }).keyup();

  $('#techinical_assistance').keyup(function() {

    // If value is not empty
    if ($(this).val().length == 0) {
      // Hide the element
      $('.techinical_assistances').hide();

    } else {
      // Otherwise show it
      $('.techinical_assistances').show();
    }
  }).keyup();

  $('#judge_community').keyup(function() {

    // If value is not empty
    if ($(this).val().length == 0) {
      // Hide the element
      $('.judge_communitys').hide();

    } else {
      // Otherwise show it
      $('.judge_communitys').show();
    }
  }).keyup();

  $('#commencement_guest_speaker').keyup(function() {

    // If value is not empty
    if ($(this).val().length == 0) {
      // Hide the element
      $('.commencement_guest_speakers').hide();

    } else {
      // Otherwise show it
      $('.commencement_guest_speakers').show();
    }
  }).keyup();
  $('#coordinator_organizer_consultants').keyup(function() {

    // If value is not empty
    if ($(this).val().length == 0) {
      // Hide the element
      $('.coordinator_organizer_consultantses').hide();

    } else {
      // Otherwise show it
      $('.coordinator_organizer_consultantses').show();
    }
  }).keyup();
  $('#resource_person_lecturer').keyup(function() {

    // If value is not empty
    if ($(this).val().length == 0) {
      // Hide the element
      $('.resource_person_lecturers').hide();

    } else {
      // Otherwise show it
      $('.resource_person_lecturers').show();
    }
  }).keyup();
  $('#facilitator').keyup(function() {

    // If value is not empty
    if ($(this).val().length == 0) {
      // Hide the element
      $('.facilitators').hide();

    } else {
      // Otherwise show it
      $('.facilitators').show();
    }
  }).keyup();
  $('#member').keyup(function() {

    // If value is not empty
    if ($(this).val().length == 0) {
      // Hide the element
      $('.members').hide();

    } else {
      // Otherwise show it
      $('.members').show();
    }
  }).keyup();


